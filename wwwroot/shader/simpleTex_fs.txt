precision mediump float; // required

uniform sampler2D u_Sampler;
uniform vec4 u_Color;        // if texture isn't available/ready, set solid color
varying vec2 v_TextureCoord;

void main() {
	gl_FragColor = texture2D(u_Sampler, v_TextureCoord) * u_Color;
}