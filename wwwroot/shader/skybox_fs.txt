precision mediump float; // required

uniform samplerCube u_Skybox;
uniform mat4 u_MV; // model-view
varying vec4 v_Position;

void main() {
	vec4 coord = u_MV * v_Position;
	vec4 color = textureCube(u_Skybox, coord.xyz / coord.w);
	gl_FragColor = color;
}