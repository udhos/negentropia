attribute vec3 a_Position;
varying vec4 v_Position;
uniform mat4 u_P;  // projection
uniform mat4 u_MV; // model-view

void main() {
  vec4 coord = u_P * u_MV * vec4(a_Position, 1.0);
  v_Position = coord;
  gl_Position = coord;
}
