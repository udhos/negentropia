library texture;

class TextureInfo {
  
  //Map<String,TextureInfo> textureTable;
  int indexOffset;
  int indexNumber;
  String textureName;
  
  TextureInfo(Map<String,TextureInfo> textureTable, this.indexOffset, this.indexNumber, this.textureName) {
    
  }
}